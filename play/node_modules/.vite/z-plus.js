import {
  computed,
  createElementBlock,
  defineComponent,
  normalizeStyle,
  openBlock,
  renderSlot
} from "./chunk-IUCRNN7I.js";

// ../node_modules/z-plus/es/utils/with-install.js
var withInstall = (comp) => {
  comp.install = function(app) {
    app.component(comp.name, comp);
  };
  return comp;
};

// ../node_modules/z-plus/es/components/icon/index.js
var iconProps = {
  size: {
    type: Number
  },
  color: {
    type: String
  }
};
var script = defineComponent({
  name: "ZIcon",
  props: iconProps,
  setup(props) {
    const style = computed(() => {
      if (!props.size && !props.color) {
        return {};
      }
      return Object.assign(Object.assign({}, props.size ? { "font-size": props.size + "px" } : {}), props.color ? { "color": props.color } : {});
    });
    return { style };
  }
});
function render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("i", {
    class: "z-icon",
    style: normalizeStyle(_ctx.style)
  }, [
    renderSlot(_ctx.$slots, "default")
  ], 4);
}
script.render = render;
script.__file = "packages/components/icon/src/icon.vue";
var ZIcon = withInstall(script);

// ../node_modules/z-plus/es/index.js
var components = [ZIcon];
var install = (app) => {
  components.forEach((component) => app.use(component));
};
var index = {
  install
};

// dep:z-plus
var z_plus_default = index;
export {
  ZIcon,
  z_plus_default as default
};
//# sourceMappingURL=z-plus.js.map
